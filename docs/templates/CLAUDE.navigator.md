@AGENTS.md

Use the `mentu-navigator` skill and the repository-navigation contract in
`AGENTS.md` for non-trivial orientation. Prefer `mentu-nav --agent
"<question>"`; use `mentu-nav handles "<topic>"` for docs-as-code discovery,
then hydrate the selected document body. Verify important conclusions at the
returned source locations.
